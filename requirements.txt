# Quanta - x402-metered, MCP-native structured-data API
# NOTE: x402 + asyncpg ship prebuilt wheels for Python 3.11-3.12.
# Prod/Docker uses 3.12 (see Dockerfile). Local dev on 3.13/3.14 can run the
# data + MCP core against SQLite; x402 metering degrades to dev mode if the
# wheel is unavailable (see app/x402_setup.py).

fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
greenlet>=3.0          # required by SQLAlchemy async
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27

# Local dev DB
aiosqlite>=0.20
# Supabase / Postgres (prod)
asyncpg>=0.29

# Model Context Protocol SDK (FastMCP)
mcp>=1.2

# Coinbase x402 protocol SDK (EVM + Solana settlement)
# [evm] pulls eth-account + web3; [svm] pulls the solana stack.
x402[evm,svm]>=2.13
# pytest for the test suite
pytest>=8.0
