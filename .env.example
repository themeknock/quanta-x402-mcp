# ---- Database -------------------------------------------------------------
# Local dev (default): SQLite. Prod: Supabase / Postgres.
DATABASE_URL=sqlite+aiosqlite:///./quanta.db
# Supabase example:
# DATABASE_URL=postgresql+asyncpg://postgres:[PASSWORD]@db.[PROJECT].supabase.co:5432/postgres

# ---- x402 payment settlement ---------------------------------------------
X402_ENABLED=true
# Public testnet facilitator (Base Sepolia). Free, no keys needed.
X402_FACILITATOR_URL=https://www.x402.org/facilitator
# Wallet that RECEIVES the micropayments (Base Sepolia):
X402_EVM_ADDRESS=0xYourBaseSepoliaAddress
# Price charged per data call:
X402_PRICE=$0.001
# Solana rail: OFF by default (public testnet facilitator settles EVM only).
# Turn on with a Solana-capable facilitator / Coinbase CDP, then set the address.
X402_ENABLE_SVM=false
X402_SVM_ADDRESS=YourSolanaDevnetAddress
# Mainnet settlement via Coinbase CDP (leave blank to stay on testnet):
# CDP_API_KEY_ID=
# CDP_API_KEY_SECRET=

# ---- Website checks -------------------------------------------------------
CHECK_TIMEOUT_S=10
CHECK_MAX_BYTES=2097152
CHECK_MAX_REDIRECTS=5
# /demo is free and unmetered, so it only checks hosts we own.
DEMO_ALLOWLIST=themeknock.net,example.com

# ---- Rate limit (per verified payer - the payment address is the principal) --
RATE_CAPACITY=60
RATE_REFILL_PER_S=1

# ---- App ------------------------------------------------------------------
APP_ENV=dev
PORT=4021
# Public origin of this deployment. The MCP endpoint keeps DNS-rebinding
# protection on, so it only answers requests whose Host header is localhost,
# this URL's host, or something listed in MCP_EXTRA_HOSTS.
PUBLIC_BASE_URL=http://localhost:4021
MCP_EXTRA_HOSTS=
